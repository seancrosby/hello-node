# hello-node
Hello World in Node using Express Framework

# Creation Log

```bash

npm init -y # create new project
ls 
cat package.json # look at the project details
npm install express # install the express framework
git status
cat package # oops
cat package.json
vim hello.js # add initial server logic
ls
vim serve.sh # add a script for starting the service
chmod u+x serve.sh # add execute permissions
./serve.sh # start the service (ERROR!)
mv hello.js index.js # fix the name of the code file
./serve.sh # start the service

```
